name: command_injection
user_prompt: |
  ## 命令注入专项任务
  请基于公共 system prompt 中的入口函数、Function Skill 和 Code Map，判断外部输入是否能进入命令执行路径。

  重点关注：
  - 外部输入是否参与命令字符串拼接、格式化或参数组装。
  - 是否存在白名单、参数化执行、严格枚举或安全转义。
  - 只有能证明当前入口函数外部输入可达危险命令执行点时，才判定为漏洞。
