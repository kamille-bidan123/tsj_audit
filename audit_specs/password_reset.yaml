name: password_reset
user_prompt: |
  ## 密码重置专项任务
  请基于公共 system prompt 中的入口函数、Function Skill 和 Code Map，判断该入口是否允许绕过身份校验重置密码。

  重点关注：
  - 是否需要旧密码、一次性 token、验证码、安全问题或等价身份校验。
  - token/验证码是否过期、单次使用、绑定账号和场景。
  - 如果当前入口不是密码重置场景，应返回无漏洞或低置信度。
