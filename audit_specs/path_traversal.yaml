name: path_traversal
user_prompt: |
  ## 路径遍历专项任务
  请基于公共 system prompt 中的入口函数、Function Skill 和 Code Map，判断外部输入是否能影响文件路径或文件操作。

  重点关注：
  - 外部输入是否参与路径拼接、路径选择、文件名生成或目录切换。
  - 是否存在路径规范化、目录边界校验、白名单、符号链接约束。
  - 只有能证明当前入口函数外部输入可达文件操作点时，才判定为漏洞。
